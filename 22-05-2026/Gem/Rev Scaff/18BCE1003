You are designing a system that processes event logs generated by two sensors in a factory. Based on the given test cases, identify the policy used to process and combine these logs.

The factory maintains a centralized monitoring dashboard. Event logs from both sensors are aggregated using the following rules:

every distinct event should appear exactly once,
duplicate entries must be eliminated,
and the final dashboard should display event IDs in sorted order.
